# Down News — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bot Discord que monitora status de Claude, ChatGPT, Gemini, Microsoft, AWS, GCP, Cloudflare, Hetzner, GitHub e Discord — alertando canais separados com @here em incidentes críticos, via GitHub Actions (gratuito, cron a cada 5 min).

**Architecture:** GitHub Actions roda `monitor.py` a cada 5 minutos. O script consulta APIs de status (statuspage.io JSON + adapters customizados) e scrapa o Downdetector BR. Compara resultado com `state.json` persistido no branch `status-state`. Envia webhooks Discord apenas quando detecta mudança de estado.

**Tech Stack:** Python 3.13, requests, beautifulsoup4, pytest, GitHub Actions, Discord Webhooks

---

## Arquivo de Configuração (config.yml)

Todos os serviços, URLs, canais e thresholds ficam aqui. Adapters lêem daqui, não têm URLs hardcoded.

---

## Task 1: Setup inicial + config.yml

**Files:**
- Create: `config.yml`
- Create: `requirements.txt`
- Create: `requirements-dev.txt`

- [ ] **Step 1: Criar config.yml com todos os serviços**

```yaml
# config.yml
services:
  claude:
    name: "Claude (Anthropic)"
    type: statuspage
    base_url: "https://status.claude.com"
    discord_channel: dn-claude
    downdetector_slug: "chatbot-anthropic"

  openai:
    name: "ChatGPT (OpenAI)"
    type: statuspage
    base_url: "https://status.openai.com"
    discord_channel: dn-openai
    downdetector_slug: "chatgpt"

  gemini:
    name: "Gemini (Google)"
    type: google
    base_url: "https://status.google.com"
    discord_channel: dn-gemini
    downdetector_slug: "google"

  office365:
    name: "Office 365 / Teams"
    type: microsoft
    base_url: "https://status.office.com"
    discord_channel: dn-microsoft
    downdetector_slug: "microsoft-office-365"

  azure:
    name: "Azure"
    type: azure
    base_url: "https://status.azure.com"
    discord_channel: dn-microsoft
    downdetector_slug: "microsoft-azure"

  aws:
    name: "AWS"
    type: aws
    base_url: "https://health.aws.amazon.com/health/status"
    discord_channel: dn-cloud
    downdetector_slug: "amazon-web-services"

  gcp:
    name: "GCP"
    type: google_cloud
    base_url: "https://status.cloud.google.com"
    discord_channel: dn-cloud
    downdetector_slug: "google-cloud"

  cloudflare:
    name: "Cloudflare"
    type: statuspage
    base_url: "https://www.cloudflarestatus.com"
    discord_channel: dn-cloud
    downdetector_slug: "cloudflare"

  hetzner:
    name: "Hetzner"
    type: statuspage
    base_url: "https://status.hetzner.com"
    discord_channel: dn-cloud
    downdetector_slug: "hetzner-online"

  github:
    name: "GitHub"
    type: statuspage
    base_url: "https://githubstatus.com"
    discord_channel: dn-dev
    downdetector_slug: "github"

  discord:
    name: "Discord"
    type: statuspage
    base_url: "https://discordstatus.com"
    discord_channel: dn-dev
    downdetector_slug: "discord"

thresholds:
  downdetector_spike_ratio: 5.0      # 500% acima da baseline = crítico
  downdetector_baseline_hours: 24
  request_timeout: 10                # segundos
  retry_attempts: 3
  retry_backoff: 2                   # segundos base

discord:
  mention: "@here"
  timezone: "America/Sao_Paulo"
  delay_between_webhooks: 1          # segundos entre envios
```

- [ ] **Step 2: Criar requirements.txt**

```
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
pyyaml==6.0.2
pytz==2024.1
```

- [ ] **Step 3: Criar requirements-dev.txt**

```
-r requirements.txt
pytest==8.3.4
pytest-mock==3.14.0
responses==0.25.3
```

- [ ] **Step 4: Inicializar git e criar branch status-state**

```bash
cd /home/asm444/Projetos/down-news
git init
git checkout -b main
git add config.yml requirements.txt requirements-dev.txt
git commit -m "chore: setup inicial do projeto Down News"
git checkout -b status-state
echo '{"last_check": null, "services": {}}' > state.json
git add state.json
git commit -m "chore: inicializar state.json vazio"
git checkout main
```

---

## Task 2: Base ServiceAdapter + adapter statuspage.io

**Files:**
- Create: `services/__init__.py`
- Create: `services/base.py`
- Create: `services/statuspage.py`
- Create: `tests/test_statuspage.py`

- [ ] **Step 1: Escrever teste para statuspage adapter**

```python
# tests/test_statuspage.py
import pytest
import responses
import json
from services.statuspage import StatuspageAdapter

MOCK_SUMMARY = {
    "status": {"indicator": "minor", "description": "Minor Service Outage"},
    "components": [
        {"name": "API", "status": "degraded_performance", "id": "abc123"},
        {"name": "Web", "status": "operational", "id": "def456"},
    ],
    "incidents": [
        {"id": "inc1", "name": "API Slowness", "status": "investigating",
         "impact": "minor", "shortlink": "https://stspg.io/inc1"}
    ]
}

@responses.activate
def test_fetch_retorna_status_normalizado():
    responses.add(
        responses.GET,
        "https://status.openai.com/api/v2/summary.json",
        json=MOCK_SUMMARY,
        status=200
    )
    adapter = StatuspageAdapter("openai", {"base_url": "https://status.openai.com"})
    result = adapter.fetch()
    assert result["status"] == "minor"
    assert result["components"]["API"] == "degraded_performance"
    assert len(result["incidents"]) == 1
    assert result["incidents"][0]["name"] == "API Slowness"

@responses.activate
def test_fetch_retorna_none_em_timeout():
    responses.add(
        responses.GET,
        "https://status.openai.com/api/v2/summary.json",
        body=Exception("Connection timeout")
    )
    adapter = StatuspageAdapter("openai", {"base_url": "https://status.openai.com"})
    result = adapter.fetch()
    assert result is None
```

- [ ] **Step 2: Rodar teste — deve falhar**

```bash
cd /home/asm444/Projetos/down-news
python -m venv venv && source venv/bin/activate
pip install -r requirements-dev.txt
pytest tests/test_statuspage.py -v
```
Esperado: `ImportError: No module named 'services.statuspage'`

- [ ] **Step 3: Implementar base.py e statuspage.py**

```python
# services/base.py
from abc import ABC, abstractmethod
from typing import Optional

class ServiceAdapter(ABC):
    def __init__(self, service_id: str, config: dict):
        self.service_id = service_id
        self.config = config

    @abstractmethod
    def fetch(self) -> Optional[dict]:
        """Retorna dict normalizado ou None se falhar."""
        pass
```

```python
# services/statuspage.py
import requests
from typing import Optional
from .base import ServiceAdapter

class StatuspageAdapter(ServiceAdapter):
    def fetch(self) -> Optional[dict]:
        url = f"{self.config['base_url']}/api/v2/summary.json"
        try:
            resp = requests.get(url, timeout=10)
            resp.raise_for_status()
            data = resp.json()
            return {
                "status": data["status"]["indicator"],
                "description": data["status"]["description"],
                "components": {
                    c["name"]: c["status"] for c in data.get("components", [])
                },
                "incidents": [
                    {
                        "id": inc["id"],
                        "name": inc["name"],
                        "status": inc["status"],
                        "impact": inc["impact"],
                        "link": inc.get("shortlink", ""),
                    }
                    for inc in data.get("incidents", [])
                ],
            }
        except Exception:
            return None
```

- [ ] **Step 4: Rodar testes — devem passar**

```bash
pytest tests/test_statuspage.py -v
```
Esperado: `2 passed`

- [ ] **Step 5: Commit**

```bash
git add services/ tests/test_statuspage.py
git commit -m "feat: adapter statuspage.io com testes"
```

---

## Task 3: Adapters Google, Microsoft, AWS

**Files:**
- Create: `services/google.py`
- Create: `services/microsoft.py`
- Create: `services/aws.py`
- Create: `tests/test_adapters_custom.py`

- [ ] **Step 1: Escrever testes para os adapters customizados**

```python
# tests/test_adapters_custom.py
import responses
import pytest
from services.google import GoogleAdapter
from services.microsoft import MicrosoftAdapter
from services.aws import AWSAdapter

# Google retorna JSON com lista de serviços
GOOGLE_MOCK = {
    "services": [
        {"id": "gemini", "name": "Gemini API", "status": {"id": "AVAILABLE"}},
        {"id": "ai_studio", "name": "AI Studio", "status": {"id": "SERVICE_DISRUPTION"}},
    ]
}

@responses.activate
def test_google_normaliza_status():
    responses.add(responses.GET,
        "https://status.google.com/api/v2/summary.json",
        json=GOOGLE_MOCK, status=200)
    adapter = GoogleAdapter("gemini", {"base_url": "https://status.google.com"})
    result = adapter.fetch()
    assert result is not None
    assert result["status"] in ("operational", "degraded", "outage")

@responses.activate
def test_aws_retorna_none_em_erro():
    responses.add(responses.GET,
        "https://health.aws.amazon.com/health/status",
        body=Exception("timeout"))
    adapter = AWSAdapter("aws", {"base_url": "https://health.aws.amazon.com/health/status"})
    result = adapter.fetch()
    assert result is None
```

- [ ] **Step 2: Rodar — deve falhar**

```bash
pytest tests/test_adapters_custom.py -v
```

- [ ] **Step 3: Implementar google.py**

```python
# services/google.py
import requests
from typing import Optional
from .base import ServiceAdapter

STATUS_MAP = {
    "AVAILABLE": "operational",
    "SERVICE_INFORMATION": "operational",
    "SERVICE_DISRUPTION": "degraded",
    "SERVICE_OUTAGE": "outage",
}

class GoogleAdapter(ServiceAdapter):
    def fetch(self) -> Optional[dict]:
        url = f"{self.config['base_url']}/api/v2/summary.json"
        try:
            resp = requests.get(url, timeout=10)
            resp.raise_for_status()
            data = resp.json()
            services = data.get("services", [])
            statuses = [STATUS_MAP.get(s["status"]["id"], "unknown") for s in services]
            if "outage" in statuses:
                overall = "outage"
            elif "degraded" in statuses:
                overall = "degraded"
            else:
                overall = "operational"
            return {
                "status": overall,
                "description": f"{sum(1 for s in statuses if s != 'operational')} serviço(s) afetado(s)",
                "components": {s["name"]: STATUS_MAP.get(s["status"]["id"], "unknown") for s in services},
                "incidents": [],
            }
        except Exception:
            return None
```

- [ ] **Step 4: Implementar microsoft.py**

```python
# services/microsoft.py
import requests
from typing import Optional
from .base import ServiceAdapter

class MicrosoftAdapter(ServiceAdapter):
    def fetch(self) -> Optional[dict]:
        # Microsoft Status usa RSS/JSON próprio
        url = f"{self.config['base_url']}/api/MSCommerce/2016/cloud/products"
        try:
            resp = requests.get(url, timeout=10)
            resp.raise_for_status()
            data = resp.json()
            workloads = data.get("WorkloadStatus", [])
            has_incident = any(
                w.get("StatusDisplayName", "") not in ("Normal service", "")
                for w in workloads
            )
            status = "degraded" if has_incident else "operational"
            return {
                "status": status,
                "description": "Microsoft 365 Services",
                "components": {
                    w["WorkloadDisplayName"]: (
                        "operational" if w.get("StatusDisplayName") == "Normal service" else "degraded"
                    )
                    for w in workloads
                },
                "incidents": [],
            }
        except Exception:
            return None
```

- [ ] **Step 5: Implementar aws.py**

```python
# services/aws.py
import requests
from typing import Optional
from .base import ServiceAdapter

class AWSAdapter(ServiceAdapter):
    def fetch(self) -> Optional[dict]:
        try:
            resp = requests.get(self.config["base_url"], timeout=10)
            resp.raise_for_status()
            data = resp.json()
            # AWS Health Dashboard: procura por eventos ativos
            events = data.get("events", [])
            active = [e for e in events if e.get("statusCode") != "closed"]
            status = "operational" if not active else "degraded"
            return {
                "status": status,
                "description": f"{len(active)} evento(s) ativo(s)",
                "components": {},
                "incidents": [{"id": e["arn"], "name": e.get("eventTypeCode", ""), "status": e.get("statusCode", ""), "impact": "unknown", "link": ""} for e in active[:5]],
            }
        except Exception:
            return None
```

- [ ] **Step 6: Rodar testes**

```bash
pytest tests/test_adapters_custom.py -v
```
Esperado: `2 passed`

- [ ] **Step 7: Commit**

```bash
git add services/google.py services/microsoft.py services/aws.py tests/test_adapters_custom.py
git commit -m "feat: adapters Google, Microsoft e AWS"
```

---

## Task 4: Downdetector BR scraper

**Files:**
- Create: `services/downdetector.py`
- Create: `tests/test_downdetector.py`

- [ ] **Step 1: Escrever teste com HTML mockado**

```python
# tests/test_downdetector.py
from unittest.mock import patch, MagicMock
from services.downdetector import DowndetectorScraper

MOCK_HTML = """
<html><body>
<div class="charts-row">
  <script>
    var g_chart_data = [{"x":1710000000,"y":45},{"x":1710000300,"y":890}];
  </script>
</div>
</body></html>
"""

def test_extrai_relatos_recentes():
    with patch("requests.get") as mock_get:
        mock_resp = MagicMock()
        mock_resp.text = MOCK_HTML
        mock_resp.status_code = 200
        mock_get.return_value = mock_resp

        scraper = DowndetectorScraper()
        result = scraper.fetch("chatgpt")

    assert result is not None
    assert "reports_1h" in result
    assert isinstance(result["reports_1h"], int)

def test_retorna_none_em_erro():
    with patch("requests.get", side_effect=Exception("blocked")):
        scraper = DowndetectorScraper()
        result = scraper.fetch("chatgpt")
    assert result is None
```

- [ ] **Step 2: Rodar — deve falhar**

```bash
pytest tests/test_downdetector.py -v
```

- [ ] **Step 3: Implementar downdetector.py**

```python
# services/downdetector.py
import re
import json
import requests
from typing import Optional
from datetime import datetime, timezone, timedelta

class DowndetectorScraper:
    BASE_URL = "https://downdetector.com.br/status/{slug}/"
    HEADERS = {
        "User-Agent": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 "
                      "(KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
        "Accept-Language": "pt-BR,pt;q=0.9",
    }

    def fetch(self, slug: str) -> Optional[dict]:
        url = self.BASE_URL.format(slug=slug)
        try:
            resp = requests.get(url, headers=self.HEADERS, timeout=15)
            resp.raise_for_status()
            # Extrai array de dados do gráfico via regex
            match = re.search(r"var g_chart_data\s*=\s*(\[.*?\]);", resp.text, re.DOTALL)
            if not match:
                return {"reports_1h": 0, "baseline": 0, "spike_ratio": 1.0}

            data_points = json.loads(match.group(1))
            now = datetime.now(timezone.utc)
            one_hour_ago = now - timedelta(hours=1)
            one_day_ago = now - timedelta(hours=24)

            reports_1h = sum(
                p["y"] for p in data_points
                if datetime.fromtimestamp(p["x"], tz=timezone.utc) >= one_hour_ago
            )
            reports_24h = sum(
                p["y"] for p in data_points
                if datetime.fromtimestamp(p["x"], tz=timezone.utc) >= one_day_ago
            )
            baseline = reports_24h / 24 if reports_24h else 1
            spike_ratio = reports_1h / baseline if baseline > 0 else 1.0

            return {
                "reports_1h": int(reports_1h),
                "baseline": round(baseline, 1),
                "spike_ratio": round(spike_ratio, 2),
            }
        except Exception:
            return None
```

- [ ] **Step 4: Rodar testes**

```bash
pytest tests/test_downdetector.py -v
```
Esperado: `2 passed`

- [ ] **Step 5: Commit**

```bash
git add services/downdetector.py tests/test_downdetector.py
git commit -m "feat: scraper Downdetector BR"
```

---

## Task 5: StateManager (lê/escreve state.json no branch status-state)

**Files:**
- Create: `state_manager.py`
- Create: `tests/test_state_manager.py`

- [ ] **Step 1: Escrever testes**

```python
# tests/test_state_manager.py
import json
import pytest
from unittest.mock import patch, MagicMock
from state_manager import StateManager

MOCK_STATE = {
    "last_check": "2026-03-18T14:00:00Z",
    "services": {
        "claude": {"status": "operational", "incidents": [], "downdetector_br": {"reports_1h": 5}}
    }
}

def test_load_retorna_estado_vazio_se_arquivo_nao_existe():
    with patch("subprocess.run") as mock_run:
        mock_run.return_value = MagicMock(returncode=1, stdout="")
        sm = StateManager()
        state = sm.load()
    assert state == {"last_check": None, "services": {}}

def test_get_service_retorna_dict_vazio_para_servico_novo():
    sm = StateManager()
    sm._state = MOCK_STATE
    result = sm.get_service("novo_servico")
    assert result == {}

def test_get_service_retorna_estado_existente():
    sm = StateManager()
    sm._state = MOCK_STATE
    result = sm.get_service("claude")
    assert result["status"] == "operational"
```

- [ ] **Step 2: Rodar — deve falhar**

```bash
pytest tests/test_state_manager.py -v
```

- [ ] **Step 3: Implementar state_manager.py**

```python
# state_manager.py
import json
import subprocess
from datetime import datetime, timezone
from typing import Optional

STATE_FILE = "state.json"
STATE_BRANCH = "status-state"

class StateManager:
    def __init__(self):
        self._state: dict = {"last_check": None, "services": {}}

    def load(self) -> dict:
        result = subprocess.run(
            ["git", "show", f"{STATE_BRANCH}:{STATE_FILE}"],
            capture_output=True, text=True
        )
        if result.returncode == 0:
            try:
                self._state = json.loads(result.stdout)
            except json.JSONDecodeError:
                self._state = {"last_check": None, "services": {}}
        return self._state

    def get_service(self, service_id: str) -> dict:
        return self._state.get("services", {}).get(service_id, {})

    def update_service(self, service_id: str, data: dict):
        if "services" not in self._state:
            self._state["services"] = {}
        self._state["services"][service_id] = data

    def save(self):
        self._state["last_check"] = datetime.now(timezone.utc).isoformat()

        # Escreve state.json, commita no branch status-state
        with open(STATE_FILE, "w") as f:
            json.dump(self._state, f, indent=2, ensure_ascii=False)

        subprocess.run(["git", "fetch", "origin", STATE_BRANCH], check=False)
        subprocess.run(["git", "checkout", STATE_BRANCH], check=True)
        subprocess.run(["git", "add", STATE_FILE], check=True)
        subprocess.run([
            "git", "commit", "-m",
            f"state: update {datetime.now(timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')}"
        ], check=False)  # check=False pois pode não ter mudanças
        subprocess.run(["git", "push", "origin", STATE_BRANCH], check=False)
        subprocess.run(["git", "checkout", "main"], check=True)
```

- [ ] **Step 4: Rodar testes**

```bash
pytest tests/test_state_manager.py -v
```
Esperado: `3 passed`

- [ ] **Step 5: Commit**

```bash
git add state_manager.py tests/test_state_manager.py
git commit -m "feat: StateManager com persistência no branch status-state"
```

---

## Task 6: DiffEngine (detecta mudanças de estado)

**Files:**
- Create: `diff_engine.py`
- Create: `tests/test_diff_engine.py`

- [ ] **Step 1: Escrever testes**

```python
# tests/test_diff_engine.py
from diff_engine import DiffEngine

def test_detecta_novo_incidente():
    previous = {"status": "operational", "incidents": []}
    current = {
        "status": "minor",
        "incidents": [{"id": "inc1", "name": "API Slowness", "impact": "minor", "link": ""}]
    }
    engine = DiffEngine()
    changes = engine.diff("claude", previous, current)
    assert len(changes) == 1
    assert changes[0]["type"] == "new_incident"

def test_detecta_mudanca_de_status():
    previous = {"status": "operational", "incidents": []}
    current = {"status": "critical", "incidents": []}
    engine = DiffEngine()
    changes = engine.diff("openai", previous, current)
    assert any(c["type"] == "status_change" for c in changes)

def test_sem_mudanca_retorna_lista_vazia():
    state = {"status": "operational", "incidents": []}
    engine = DiffEngine()
    changes = engine.diff("github", state, state)
    assert changes == []

def test_detecta_spike_downdetector():
    previous = {"status": "operational", "incidents": [], "downdetector_br": {"spike_ratio": 1.2}}
    current = {"status": "operational", "incidents": [], "downdetector_br": {"spike_ratio": 6.5, "reports_1h": 1200}}
    engine = DiffEngine(downdetector_threshold=5.0)
    changes = engine.diff("claude", previous, current)
    assert any(c["type"] == "downdetector_spike" for c in changes)

def test_resolucao_de_incidente():
    previous = {"status": "minor", "incidents": [{"id": "inc1", "name": "Slowness", "impact": "minor", "link": ""}]}
    current = {"status": "operational", "incidents": []}
    engine = DiffEngine()
    changes = engine.diff("openai", previous, current)
    assert any(c["type"] == "resolved" for c in changes)
```

- [ ] **Step 2: Rodar — deve falhar**

```bash
pytest tests/test_diff_engine.py -v
```

- [ ] **Step 3: Implementar diff_engine.py**

```python
# diff_engine.py
from typing import List

CRITICAL_STATUSES = {"critical", "major_outage", "outage"}

class DiffEngine:
    def __init__(self, downdetector_threshold: float = 5.0):
        self.downdetector_threshold = downdetector_threshold

    def diff(self, service_id: str, previous: dict, current: dict) -> List[dict]:
        if not previous:
            return []  # Primeiro run — sem histórico para comparar

        changes = []

        # Mudança de status global
        prev_status = previous.get("status", "operational")
        curr_status = current.get("status", "operational")
        if prev_status != curr_status:
            if curr_status == "operational":
                changes.append({"type": "resolved", "service": service_id,
                                 "from": prev_status, "to": curr_status})
            else:
                changes.append({"type": "status_change", "service": service_id,
                                 "from": prev_status, "to": curr_status,
                                 "critical": curr_status in CRITICAL_STATUSES})

        # Novos incidentes
        prev_incident_ids = {i["id"] for i in previous.get("incidents", [])}
        for incident in current.get("incidents", []):
            if incident["id"] not in prev_incident_ids:
                changes.append({"type": "new_incident", "service": service_id,
                                 "incident": incident,
                                 "critical": incident.get("impact") in ("critical", "major")})

        # Incidentes resolvidos (desapareceram da lista)
        curr_incident_ids = {i["id"] for i in current.get("incidents", [])}
        for incident in previous.get("incidents", []):
            if incident["id"] not in curr_incident_ids and curr_status == "operational":
                changes.append({"type": "resolved", "service": service_id,
                                 "incident": incident, "critical": False})

        # Spike no Downdetector
        prev_dd = previous.get("downdetector_br", {})
        curr_dd = current.get("downdetector_br", {})
        prev_ratio = prev_dd.get("spike_ratio", 1.0)
        curr_ratio = curr_dd.get("spike_ratio", 1.0)
        if curr_ratio >= self.downdetector_threshold and prev_ratio < self.downdetector_threshold:
            changes.append({"type": "downdetector_spike", "service": service_id,
                             "spike_ratio": curr_ratio,
                             "reports_1h": curr_dd.get("reports_1h", 0),
                             "critical": True})

        return changes
```

- [ ] **Step 4: Rodar testes**

```bash
pytest tests/test_diff_engine.py -v
```
Esperado: `5 passed`

- [ ] **Step 5: Commit**

```bash
git add diff_engine.py tests/test_diff_engine.py
git commit -m "feat: DiffEngine para detectar mudanças de estado"
```

---

## Task 7: DiscordNotifier (envia webhooks formatados)

**Files:**
- Create: `discord_notifier.py`
- Create: `tests/test_discord_notifier.py`

- [ ] **Step 1: Escrever testes**

```python
# tests/test_discord_notifier.py
import responses
import pytest
from discord_notifier import DiscordNotifier

WEBHOOKS = {
    "dn-claude": "https://discord.com/api/webhooks/123/abc",
    "dn-geral": "https://discord.com/api/webhooks/456/def",
}

CHANGE_CRITICAL = {
    "type": "status_change",
    "service": "claude",
    "from": "operational",
    "to": "major_outage",
    "critical": True,
}

CHANGE_RESOLVED = {
    "type": "resolved",
    "service": "openai",
    "from": "minor",
    "to": "operational",
    "critical": False,
}

@responses.activate
def test_envia_webhook_para_canal_correto():
    responses.add(responses.POST, WEBHOOKS["dn-claude"], json={}, status=204)
    responses.add(responses.POST, WEBHOOKS["dn-geral"], json={}, status=204)

    notifier = DiscordNotifier(WEBHOOKS)
    notifier.notify(
        change=CHANGE_CRITICAL,
        service_config={"name": "Claude", "discord_channel": "dn-claude",
                        "base_url": "https://status.claude.com"},
        current_state={"downdetector_br": {"reports_1h": 500, "spike_ratio": 6.0}},
    )
    # Canal do serviço + #dn-geral (crítico)
    assert len(responses.calls) == 2

@responses.activate
def test_resolved_envia_apenas_canal_servico():
    responses.add(responses.POST, WEBHOOKS["dn-claude"], json={}, status=204)
    notifier = DiscordNotifier(WEBHOOKS)
    notifier.notify(
        change=CHANGE_RESOLVED,
        service_config={"name": "OpenAI", "discord_channel": "dn-claude",
                        "base_url": "https://status.openai.com"},
        current_state={},
    )
    assert len(responses.calls) == 1
```

- [ ] **Step 2: Rodar — deve falhar**

```bash
pytest tests/test_discord_notifier.py -v
```

- [ ] **Step 3: Implementar discord_notifier.py**

```python
# discord_notifier.py
import time
import requests
from datetime import datetime
from zoneinfo import ZoneInfo

ICONS = {
    "status_change_critical": "🔴",
    "status_change": "🟠",
    "maintenance": "🟡",
    "resolved": "🟢",
    "downdetector_spike": "🔴",
    "new_incident": "🟠",
}

COLORS = {
    "critical": 0xFF0000,
    "degraded": 0xFF8C00,
    "maintenance": 0xFFD700,
    "resolved": 0x00C853,
}

BRT = ZoneInfo("America/Sao_Paulo")

class DiscordNotifier:
    def __init__(self, webhooks: dict, delay: float = 1.0):
        self.webhooks = webhooks  # {"dn-claude": "https://...", ...}
        self.delay = delay

    def notify(self, change: dict, service_config: dict, current_state: dict):
        embed = self._build_embed(change, service_config, current_state)
        channel = service_config.get("discord_channel", "dn-geral")
        is_critical = change.get("critical", False)
        content = "@here" if is_critical else ""

        self._send(channel, content, embed)
        time.sleep(self.delay)

        if is_critical and channel != "dn-geral":
            self._send("dn-geral", "@here", embed)
            time.sleep(self.delay)

    def _send(self, channel: str, content: str, embed: dict):
        webhook_url = self.webhooks.get(channel)
        if not webhook_url:
            return
        payload = {"content": content, "embeds": [embed]}
        try:
            requests.post(webhook_url, json=payload, timeout=10)
        except Exception:
            pass

    def _build_embed(self, change: dict, service_config: dict, current_state: dict) -> dict:
        now_brt = datetime.now(BRT).strftime("%H:%M BRT")
        change_type = change.get("type", "")
        service_name = service_config.get("name", change["service"])
        base_url = service_config.get("base_url", "")
        dd = current_state.get("downdetector_br", {})

        if change_type == "resolved":
            icon = ICONS["resolved"]
            title = f"{icon} [RESOLVIDO] {service_name} — Serviço normalizado"
            color = COLORS["resolved"]
            description = f"⏱ Status anterior: `{change.get('from', 'degraded')}`\n🕐 Resolvido: {now_brt}"
        elif change_type == "downdetector_spike":
            icon = "🔴"
            title = f"{icon} [SPIKE BR] {service_name} — Alto volume de relatos"
            color = COLORS["critical"]
            description = (
                f"📣 Relatos na última hora: **{dd.get('reports_1h', 0)}**\n"
                f"📊 Spike: **{change.get('spike_ratio', 0):.1f}x** acima do normal\n"
                f"🕐 Detectado: {now_brt}"
            )
        elif change_type in ("status_change", "new_incident"):
            is_critical = change.get("critical", False)
            label = "CRÍTICO" if is_critical else "DEGRADADO"
            icon = ICONS["status_change_critical"] if is_critical else ICONS["status_change"]
            title = f"{icon} [{label}] {service_name}"
            color = COLORS["critical"] if is_critical else COLORS["degraded"]
            incident = change.get("incident", {})
            lines = [
                f"📊 Status: `{change.get('from', '?')}` → `{change.get('to', '?')}`",
                f"🕐 Detectado: {now_brt}",
            ]
            if incident.get("name"):
                lines.insert(0, f"📍 Incidente: {incident['name']}")
            if dd.get("reports_1h"):
                lines.append(f"📣 Downdetector BR: **{dd['reports_1h']}** relatos (+{(dd.get('spike_ratio',1)-1)*100:.0f}%)")
            if base_url:
                lines.append(f"🔗 {base_url}")
            description = "\n".join(lines)
        else:
            title = f"ℹ️ {service_name} — Atualização"
            color = 0x7289DA
            description = str(change)

        return {"title": title, "description": description, "color": color}
```

- [ ] **Step 4: Rodar testes**

```bash
pytest tests/test_discord_notifier.py -v
```
Esperado: `2 passed`

- [ ] **Step 5: Commit**

```bash
git add discord_notifier.py tests/test_discord_notifier.py
git commit -m "feat: DiscordNotifier com embeds formatados e @here em críticos"
```

---

## Task 8: Orquestrador monitor.py

**Files:**
- Create: `monitor.py`
- Create: `tests/test_monitor.py`

- [ ] **Step 1: Escrever teste de integração leve**

```python
# tests/test_monitor.py
from unittest.mock import patch, MagicMock
from monitor import run_monitor

def test_monitor_sem_mudancas_nao_envia_webhook():
    state_identico = {"status": "operational", "incidents": [], "downdetector_br": {"spike_ratio": 1.0}}
    with patch("monitor.StateManager") as MockSM, \
         patch("monitor.DiscordNotifier") as MockDN, \
         patch("monitor.DiffEngine") as MockDE, \
         patch("monitor.load_config") as MockCfg:

        MockCfg.return_value = {
            "services": {"claude": {"name": "Claude", "type": "statuspage",
                                     "base_url": "https://status.claude.com",
                                     "discord_channel": "dn-claude",
                                     "downdetector_slug": "chatbot-anthropic"}},
            "thresholds": {"downdetector_spike_ratio": 5.0,
                           "downdetector_baseline_hours": 24,
                           "request_timeout": 10, "retry_attempts": 3, "retry_backoff": 2},
            "discord": {"mention": "@here", "timezone": "America/Sao_Paulo", "delay_between_webhooks": 0},
        }

        mock_sm = MockSM.return_value
        mock_sm.load.return_value = {"last_check": None, "services": {}}
        mock_sm.get_service.return_value = state_identico

        MockDE.return_value.diff.return_value = []  # sem mudanças

        with patch("monitor.StatuspageAdapter") as MockAdapter:
            MockAdapter.return_value.fetch.return_value = state_identico
            with patch("monitor.DowndetectorScraper") as MockDD:
                MockDD.return_value.fetch.return_value = {"reports_1h": 5, "baseline": 5, "spike_ratio": 1.0}
                run_monitor()

        MockDN.return_value.notify.assert_not_called()
```

- [ ] **Step 2: Rodar — deve falhar**

```bash
pytest tests/test_monitor.py -v
```

- [ ] **Step 3: Implementar monitor.py**

```python
# monitor.py
import os
import yaml
import time
from typing import Optional
from state_manager import StateManager
from diff_engine import DiffEngine
from discord_notifier import DiscordNotifier
from services.statuspage import StatuspageAdapter
from services.google import GoogleAdapter
from services.microsoft import MicrosoftAdapter
from services.aws import AWSAdapter
from services.downdetector import DowndetectorScraper

ADAPTER_MAP = {
    "statuspage": StatuspageAdapter,
    "google": GoogleAdapter,
    "google_cloud": GoogleAdapter,
    "microsoft": MicrosoftAdapter,
    "azure": MicrosoftAdapter,
    "aws": AWSAdapter,
}

def load_config(path: str = "config.yml") -> dict:
    with open(path) as f:
        return yaml.safe_load(f)

def build_webhooks(config: dict) -> dict:
    channels = ["dn-claude", "dn-openai", "dn-gemini", "dn-microsoft",
                "dn-cloud", "dn-dev", "dn-geral"]
    webhooks = {}
    for ch in channels:
        env_key = f"DISCORD_WEBHOOK_{ch.upper().replace('-', '_')}"
        url = os.environ.get(env_key)
        if url:
            webhooks[ch] = url
    return webhooks

def run_monitor():
    config = load_config()
    sm = StateManager()
    sm.load()

    thresholds = config.get("thresholds", {})
    dd_threshold = thresholds.get("downdetector_spike_ratio", 5.0)
    delay = config.get("discord", {}).get("delay_between_webhooks", 1)

    engine = DiffEngine(downdetector_threshold=dd_threshold)
    webhooks = build_webhooks(config)
    notifier = DiscordNotifier(webhooks, delay=delay)
    dd_scraper = DowndetectorScraper()

    for service_id, svc_config in config.get("services", {}).items():
        adapter_class = ADAPTER_MAP.get(svc_config["type"])
        if not adapter_class:
            continue

        adapter = adapter_class(service_id, svc_config)
        current = adapter.fetch()
        if current is None:
            continue  # API indisponível — não altera estado

        # Adiciona dados do Downdetector
        dd_slug = svc_config.get("downdetector_slug")
        if dd_slug:
            dd_data = dd_scraper.fetch(dd_slug)
            if dd_data:
                current["downdetector_br"] = dd_data

        previous = sm.get_service(service_id)
        changes = engine.diff(service_id, previous, current)

        for change in changes:
            notifier.notify(change, svc_config, current)

        sm.update_service(service_id, current)

    sm.save()

if __name__ == "__main__":
    run_monitor()
```

- [ ] **Step 4: Rodar testes**

```bash
pytest tests/test_monitor.py -v
```
Esperado: `1 passed`

- [ ] **Step 5: Rodar suite completa**

```bash
pytest tests/ -v
```
Esperado: todos os testes passando.

- [ ] **Step 6: Commit**

```bash
git add monitor.py tests/test_monitor.py
git commit -m "feat: orquestrador monitor.py completo"
```

---

## Task 9: GitHub Actions workflow

**Files:**
- Create: `.github/workflows/monitor.yml`

- [ ] **Step 1: Criar workflow**

```yaml
# .github/workflows/monitor.yml
name: Down News Monitor

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Configurar git
        run: |
          git config user.name "down-news[bot]"
          git config user.email "down-news[bot]@users.noreply.github.com"

      - name: Executar monitor
        run: python monitor.py
        env:
          DISCORD_WEBHOOK_DN_CLAUDE: ${{ secrets.DISCORD_WEBHOOK_DN_CLAUDE }}
          DISCORD_WEBHOOK_DN_OPENAI: ${{ secrets.DISCORD_WEBHOOK_DN_OPENAI }}
          DISCORD_WEBHOOK_DN_GEMINI: ${{ secrets.DISCORD_WEBHOOK_DN_GEMINI }}
          DISCORD_WEBHOOK_DN_MICROSOFT: ${{ secrets.DISCORD_WEBHOOK_DN_MICROSOFT }}
          DISCORD_WEBHOOK_DN_CLOUD: ${{ secrets.DISCORD_WEBHOOK_DN_CLOUD }}
          DISCORD_WEBHOOK_DN_DEV: ${{ secrets.DISCORD_WEBHOOK_DN_DEV }}
          DISCORD_WEBHOOK_DN_GERAL: ${{ secrets.DISCORD_WEBHOOK_DN_GERAL }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/monitor.yml
git commit -m "ci: GitHub Actions cron a cada 5 minutos"
```

---

## Task 10: README + criar repo GitHub + push

- [ ] **Step 1: Criar README.md**

Descrever o projeto, lista de serviços monitorados, como configurar os secrets do GitHub e canais do Discord.

- [ ] **Step 2: Rodar suite de testes final antes do push**

```bash
pytest tests/ -v --tb=short
```
Todos devem passar.

- [ ] **Step 3: Criar repositório no GitHub**

```bash
gh repo create down-news --public --description "Bot Discord que monitora status de IA e infra via GitHub Actions" --source . --push
```

- [ ] **Step 4: Criar branch status-state no remoto**

```bash
git push origin status-state
```

- [ ] **Step 5: Verificar Actions no GitHub**

Acessar a aba Actions do repositório e confirmar que o workflow aparece corretamente.
